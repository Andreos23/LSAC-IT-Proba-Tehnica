# LSAC-IT-Proba-Tehnica
Andrei Otetea - 2020

Precizare: Am adaugat baza de date SQL ajutordeprogramator
(CREATE DATABASE ajutordeprogramator;) si tabelul teme
(CREATE TABLE `ajutordeprogramator`.`teme` ( `id` INT NOT NULL AUTO_INCREMENT
, `materie` TEXT NOT NULL , `frustrare` TEXT NOT NULL);).


